# sandbox

Personal Node scripts and a couple of scheduled jobs. Not maintained for outside use,
no support, no releases.

## Setup

```
npm ci
npm test
```

Configuration is read from the environment; see `.env.example` for the variable names.
